<% bookArray.forEach(book => { %>
<article>
  <img src="<%= book.image_url%>" alt="<%= book.title %>">
  <h2><%= book.title %></h2>
  <h4><%= book.author %></h4>
  <p class="book-summary"><%= book.description %></p>
  <p><%= book.bookshelf %></p>
</article>
<% }) %>